# A Vercel instala a partir deste arquivo na raiz.
# Sem pandas e sem uvicorn: pandas só é usado pelo backtest (que roda na sua
# máquina) e pesa demais para o limite de tamanho da função.
fastapi>=0.115
httpx>=0.27
pydantic>=2.9
pydantic-settings>=2.6
python-dotenv>=1.0
pywebpush>=2.0
py-vapid>=1.9
sqlalchemy>=2.0
psycopg[binary]>=3.2
