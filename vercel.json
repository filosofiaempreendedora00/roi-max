{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd web && npm install && npm run build",
  "functions": {
    "app.py": {
      "maxDuration": 60,
      "excludeFiles": "{backend/tests/**,research/**,desktop/**,data/**,scripts/**,web/node_modules/**,web/src/**,**/test_*.py}"
    }
  }
}
